<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Prise de contact</title>
    </head>
    <body>
        <h1>Prise de contact</h1>
        <p>{{ data.firstname~ " "~ data.lastname }}</p>
        <p>{{ data.email }}</p>
        <p>{{ data.phone }}</p>
        <p>{{ data.content }}</p>

    </body>

</html>
